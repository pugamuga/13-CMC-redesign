import type { NextPage } from "next";
import Header from "../components/Header";

const Home: NextPage = (): JSX.Element => {
  return (
    <div className="">
     <Header/>
    </div>
  );
};

export default Home;
